# ApexHUD module template

[Russian version](README.ru.md)

Copy this directory, choose a unique reverse-DNS `manifest.id`, request only required protocol v6 scopes, and keep every runtime asset inside the module directory. Run `npm run verify` from the repository root and test the widget at real race size in mock and live telemetry.

See `docs/en/module-development.md`.
